interface:
  display_name: "Autonomous Dev Loop"
  short_description: "Autonomous dev loop with safety gates"
  default_prompt: "Use $autonomous-dev-loop to execute a scoped development objective with safety gates."

policy:
  allow_implicit_invocation: true
